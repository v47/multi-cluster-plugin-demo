apiVersion: demo.example.com/v1
kind: Widget
metadata:
  labels:
    app.kubernetes.io/name: mcr-demo
    app.kubernetes.io/managed-by: kustomize
  name: widget-sample
spec:
  # TODO(user): Add fields here
