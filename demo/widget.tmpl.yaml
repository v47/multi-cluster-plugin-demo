apiVersion: demo.example.com/v1
kind: Widget
metadata:
  name: hello
  namespace: default
spec:
  # __CLUSTER__ is replaced by the demo script with the target cluster name.
  message: "hi from __CLUSTER__"
